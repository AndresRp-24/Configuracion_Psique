import React from "react";
import ReactDOM from "react-dom";

import Configuracion1 from "./Configuracion1";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <Configuracion1 />
  </React.StrictMode>,
  rootElement
);
